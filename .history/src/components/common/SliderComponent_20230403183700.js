import React from 'react'
import Slider from '@mui/material/Slider'

const SliderComponent = () => {
  return (
    <Slider defaultValue={20} min={100} max={} aria-label='Default' valueLabelDisplay='auto' />
  )
}

export default SliderComponent